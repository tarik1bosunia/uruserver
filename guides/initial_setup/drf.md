# [DRF](https://www.django-rest-framework.org/#installation)

## installation

```bash
pip install djangorestframework
```

## Add `rest_framework` to your `INSTALLED_APPS` setting

```python
INSTALLED_APPS = [
    ...
    'rest_framework',
]
```
